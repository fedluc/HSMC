#ifndef COMPUTE_RDF_H
#define COMPUTE_RDF_H

#include <stdbool.h>

void compute_rdf(bool init);

void rdf_hist_init();

void rdf_compute_hist();

void rdf_compute_rdf();

void rdf_output(bool init);

#endif
